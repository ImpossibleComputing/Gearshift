# Gearshift phase 2 — investigate generality, mechanism, and useful economics

This packet replaces the previous **release-only** recommendation. The owner has explicitly asked to continue investigating before deciding how to publish. The original pilot and `followup_v1` remain immutable evidence.

Give Codex this instruction in the Gearshift repository:

```text
Read gearshift_phase2/CODEX_PHASE2.md and the supporting files in that folder.
This supersedes the earlier release-only instructions: continue research.
Preserve all earlier results, implement the staged experiments, and run on
available local hardware. Do not publish, push, choose a license, or incur
paid-service costs without explicit authorization. Export the compact
phase-2 review bundle, including blinded judging packets and unscored status
where external or human judgments are unavailable.
```

`CODEX_PHASE2.md` is the operational brief. `EXPERIMENT_MATRIX.md` defines the comparisons. `JUDGING.md` defines subjective evaluation. `EVIDENCE_NOTES.md` separates previous observations from new hypotheses and lists verified methodological references. `precision_illustration.json` is an approximate sample-size illustration, not a stopping rule.

The goal is not a favorable score or a particular publication date. It is to distinguish a short-answer protocol effect from a useful, transferable way to move between models.
